---
- name: Ejecuta script en localhost
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Ejecuta script en localhost
      ansible.builtin.shell: "sh prueba.sh"
      args:
        chdir: "/tmp/"
      #command: sh /tmp/prueba.sh
