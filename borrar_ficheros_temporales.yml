---
- name: Eliminar fichero
  hosts: all
# become: yes
  tasks:
    - name: imprime ruta buscada
      debug:
        msg: "/tmp/*.tmp"
    - name: Eliminar
      ansible.builtin.file:  
        path: "/tmp/*.tmp"
        state: absent
